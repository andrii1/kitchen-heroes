<h1>All the restaurants</h1>

<% @listings.each do |listing| %>
  <div>
    <h2><%= listing.name %></h2>
      ★
    <% end %>
    <%= link_to "Destroy restaurant", destroy_listing_path(listing.id), method: :delete  %>
  </div>
<% end %>
