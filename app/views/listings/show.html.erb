<div class='show container'>
  <h1><strong><%= @listing.name %></strong></h1>
  <p><strong>Description: </strong><%= @listing.description %></p>
  <p><strong>location: </strong><%= @listing.location %></p>
  <p><strong>Price: </strong><%= @listing.price %>€</p>
  <p><strong>Availability: </strong><%= @listing.availability %></p>

  <p><%= cl_image_tag @listing.photo.key, height: 300, width: 400, crop: :fill %></p>
  <div class='btn btn-secondary mr-sm-4'><%= link_to "Back to all listings", listings_path %></div>
  <div class='btn btn-secondary mr-sm-4'><%= link_to "Delete listing", listing_path(@listing), method: :delete, data: {confirm: "Are you sure?"}  %></div>
</div>
